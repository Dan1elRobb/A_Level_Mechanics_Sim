import math import tkinter as tk from tkinter import ttk import time class Outputs_GUI(tk.Tk): def __init__(self, m): super().__init__() self.m = tk.StringVar(self, m) self.title('Your Variables good sir') self.geometry('400x400') def add_labels(self): self.label = tk.Label(self, text=f'Mass: {self.m.get()}') self.label.pack() def update_m(self, val): self.m.set(val) def remove_updated_labels(self): self.label.after(1000, self.destroy()) def calc_v_over_time(angle, mew, end_time): t = 0 time_list = [] vel_list = [] while t < end_time: v = 9.8 * t * (math.sin(angle) - math.cos(angle) * mew) vel_list.append(v) time_list.append(t) t += 0.01 return vel_list, time_list time_lists = calc_v_over_time(math.pi / 6, 0.5, 10)[1] vel_lists = calc_v_over_time(math.pi / 6, 0.5, 10)[0] if __name__ == "__main__": gui = Outputs_GUI('10') for times in time_lists: gui.update_m(str(times)) gui.add_labels() gui.remove_updated_labels() gui.mainloop()